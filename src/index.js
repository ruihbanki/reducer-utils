import createStateProxy from './state-proxy';
import reducerStateProxy from './reducer-state-proxy';

export {
    createStateProxy,
    reducerStateProxy,
};

export default {
    createStateProxy,
    reducerStateProxy,
};